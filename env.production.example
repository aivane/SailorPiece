# Server Port inside the container (default is 3000)
PORT=3000

# SlipOK config (API verification credentials)
SLIPOK_ENDPOINT=https://api.slipok.com/api/line/apikey/64332
SLIPOK_API_KEY=SLIPOKY7CM3G9

# Firebase Admin Service Account Credentials (JSON String)
# IMPORTANT: Put the entire JSON file content into single quotes on a single line.
# Example: FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'
FIREBASE_SERVICE_ACCOUNT='{"your_firebase_credentials_here": "..."}'
